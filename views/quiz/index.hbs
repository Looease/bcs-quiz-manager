<table>
    <thead>
        <th>Quiz name</th>
    </thead>
    {{#each quiz}}
        <tr>
            <td><a href="{{this.router.quizStart}}">{{this.name}}</a></td>
        </tr>
    {{/each}}
</table>